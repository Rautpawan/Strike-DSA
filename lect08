#include<iostream>
#include<vector>
using namespace std;

void printValue(int a[], int n){
    for(int i=0; i<n; i++){
        cout << a[i] << endl;
    }
}
int main(){
    int arr[5]={11, 12, 13, 14, 15};
    printValue(arr, 5);

    return 0;
}

// Linear search on GFG(completed)

//To find missing number in array || GFG
 int missingNum(vector<int>& arr) {
        int size=arr.size();
        int ans=0;
        for(int i=0; i<size; i++){
            ans=ans^arr[i]^(i+1);
        }
        ans=ans^(size+1);
        
        return ans;
    }

 // linear search
int search(int arr[], int n, int x) {
    for(int i=0;i<n;i++){
        if(arr[i]==x){
            return i;
        }
    }
    
    return -1;
}

int main() {
    // int arr[5] = {10,20,30,40,50}

    // vector<int>arr(5);
    
    // arr[0] = 10;
    // arr[1]=20,
    // arr[2] = 5;
    // arr[3]=9;
    // arr[4] = 7;

    vector<int>arr;

    arr.push_back(90);
    arr.push_back(10);

    arr.pop_back();

    int n = arr.size();

    for(int i=0;i<n;i++){
        cout<<arr[i]<<" ";
    }

    cout<<n<<" ";
    return 0;
}
